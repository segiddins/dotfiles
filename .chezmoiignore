CLAUDE.md
Dockerfile
LICENSE
README.md
install.sh
script
support
{{ if stat (joinPath .chezmoi.homeDir ".zsh/znap") }}.zsh/znap{{ end }}
{{ if not .personal }}
Downloads
move_downloads.sh
.config/age/chezmoi_key.txt
.kube/rubygems.config.age
.local/share/atuin/key.age
.config/ngrok/ngrok.yml.age
.ssh/known_hosts.age
{{ end }}
{{ if ne .chezmoi.os "darwin" }}
sudo_pam_touchid.sh
macos_settings.sh
move_downloads.sh
Downloads
{{ end }}
{{ if env "GOOGLE_CLOUD_WORKSTATIONS" }}
# Ephemeral Persona cloud workstation: skip heavy toolchain installs and
# personal/mac-only config that doesn't belong on a work box.
.Brewfile
brew_bundle.sh
rustup_install.sh
.aws
.kube
.rubies
.ssh
.config/karabiner
.config/ngrok
.config/wxchordpro
.config/atuin
.local/share/atuin
# the tracked file has no oauth_token (real token normally lives in the
# macOS Keychain), so managing it here would reset `gh auth login` on
# every chezmoi apply
.config/gh/hosts.yml
{{ end }}
.zsh/**/*.zwc
